"""
Inefficient dictionary access 3
"""
def process_dict_2(d, keys):
    result = []
    for key in keys:
        if key in d:
            result.append(d[key])
        else:
            result.append(None)
    return result
